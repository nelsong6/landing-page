# Static-only pod — no Azure SDK calls — so workload identity isn't strictly
# needed. Keeping the ServiceAccount for pattern consistency with my-homepage.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: infra-shared
  namespace: landing-page
