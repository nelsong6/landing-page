apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: landing-page-tls
  namespace: landing-page
spec:
  secretName: landing-page-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - romaine.life
